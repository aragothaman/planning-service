version: 0.2
env:
phases:
  install:
    commands:
      - echo installing maven...
  build:
    commands:
      - echo building the justiceleague-tracker
      - mvn install
artifacts:
  files:
    - target/*.jar
    - scripts/*.sh
    - appspec.yml
  discard-paths: yes
cache:
  paths:
    - '/root/.m2/**/*'